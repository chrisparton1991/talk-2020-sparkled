import React, { useRef } from 'react'
import Theme from '../theme/Theme'

type Props = {
  theme: Theme
}

const S = {}

const Slide2: React.FC<Props> = ({ theme }) => {
  return (
    <h1>Slide 2</h1>
  )
}

export default Slide2
